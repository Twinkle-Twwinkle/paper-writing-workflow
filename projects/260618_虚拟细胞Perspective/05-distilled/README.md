# 05 · 精练

步骤5 · skill：**精练3.0**。每篇 `04-markdown-clean/` 的清洗稿 + `00-context/context.md` 一起喂给 skill，输出纯英文「源索引（Source Index）」`<basename>_refined.md` 放这里。

- 高召回逐字打捞承重原文，连出处与所触论点索引；不预写论点、不示范改写、不锁定论文角色。
- 每篇带一张 `claim-touchpoint` 路由表，键到 `00-context` 的 claim/axis 标识符，供下游 outline agent 跨论文按 claim 聚合。
