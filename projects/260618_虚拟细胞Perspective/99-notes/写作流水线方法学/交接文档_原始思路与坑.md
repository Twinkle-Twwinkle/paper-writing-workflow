# 会话交接文档

- **生成时间**：2026-06-18 15:27
- **项目根**：/Users/augustsirius/Desktop/已清洗_260617
- **交接原因**：context 偏高；P1+P2 已跑完，开新会话直接进 P3。

## 1. 任务目标（用户原始诉求）
开发一个**可复用的 Claude Code Workflow**：把 ~41 篇论文（每篇有 `_cleaned.md` 原文 + `_refined.md` 提炼）+ 一份"地基 context"，整合成一篇**可被审计的 outline 骨架**——每个论断都绑定**真实论文引用 + 我们的引用编号 + 一句逐字原文**；审计通过后能直接"编译"成完整论文。
- 成品论文 = 一篇 **1500–2000 字英文 Perspective**，立场：**AI 虚拟细胞的真正瓶颈是数据地基**——主流解离式单细胞测序丢了**空间**与**时间**，又几乎只有 RNA、缺蛋白等不可互替模态，且各自为政不可比 → 需要一套"**算据标准**"（模态 × 时空分辨 × 可整合可比）。郭天南组（西湖）有高通量时空蛋白质组学产能，正好补"蛋白+空间+时间"缺口。
- **关键原则**：context 只是**初始假设**，必须被真实论文证据**修正**（这正是 P3 要做的）。技能要做成通用、不焊死具体题目。
- 用户要"被带着搭"，每个决策点要讲清、停下让他拍板。**对话用中文**。用户=蒋恒。

## 2. 已完成的工作 + 结论

**架构（6 阶段流水线）**：P1 抽取 → P2 矩阵 → **P3 修正 thesis（下一步）** → P4 成骨 → P5 审计 → 编译 →（最后打包成可复用技能）。原则：能用确定性代码做的（P2）不派 agent。

- **P1（已重做为 v4）**：原本 P1 用 agent 把 `_refined.md` 重新解析成 JSON，发现是**冗余且有损**的往返（每 claim 只留 1 句，丢了 ~80% 证据）。用户决策：让上游"精练"技能**直接吐 JSON**。
  - 据此创建 **精练 4.0**（`_refined.json`，excerpt-centric，每条原句自带 `touchpoints:[{claimId,direction}]`，**无 strength**，JSON 单产物，加了"抑制过度打标"规则）。
  - 用户决策 **B = 重读全文**：41 个 subagent 跑 v4 读 `_cleaned.md` → `_build/v4-cards/*_refined.json`。结果：**1003 条原句 / 1391 触点**，全部 `jsonValid`。约为旧有损 P1（588 绑定）的 2.4 倍，且带 figuresNumbers + primarySources。
- **P2（已重做为 v4）**：`build_matrix_v4.py`（纯 Python）→ `evidence-matrix.md` + `citation-registry.md` + `cards.json`。
  - **抗灌水 scoreboard（按"支持论文数"计）**：Claim 9 标准缺位 31 篇 / Claim 8 整合 30 / Claim 2 丢空间 28 / Axis C 空间 27 / Claim 1 瓶颈 23 / Claim 4 模态不可替 22 …
  - **★核心发现：空间 vs 时间不对称**。空间被海量记录（28/27/19 篇）；**时间薄且最受争议**——Axis B 时间仅 **9 篇**正面支持（+20 nuance +10 challenge），Claim 3 丢时间是全场最争议（12 支持 / 30 nuance / **25 challenge**）。原因：讲时间的论文给的几乎都是**替代品**（pseudotime / RNA velocity / lineage tracing），不是真配对时间数据——workaround 扎堆本身就是缺口铁证。**→ thesis 应把"时间"写成比"空间"更硬、更结构性的缺口（这就是证据对 context 的修正）。**
  - **唯一的 `Other` 涌现主题** = 论文 03 的"**model/architecture-first** 立场（用 compositional AI 解决数据稀缺）"——这是全文的**对立论题**，开篇应点名对置。
  - **必须正面回击的反方**：论文 34（mRNA-蛋白脱钩"大半是测量噪声、非正交生物学"，直击 Claim 4）→ 用 33/35 的"真生物学脱钩"反驳 + 把噪声归到 Claim 9 标准问题。

- **质量验证结论**：
  - **逐字保真 ≈ 100%**。审计读 93%，但深挖最低的 02/28 证明：所有"未命中"都是**引用号数字/`<sup>`标签**被 agent 合理剥掉，**零改写零编造**。
  - **41 篇里 33 篇书目（期刊/DOI）是推断的**（refined 自己 flag 了）——编译成真论文前**必须核验**，这是审计红线（见 `citation-registry.md` 的 `meta?` 列）。

- **跑出来才暴露、已处理的 3 个坑**：① **claimId 碎成 60 种**（去掉 schema 枚举后 41 个 agent 各自翻译/编号）→ P2 里 `canon()` 折回 14 规范键（**创可贴**；根治=给 context 加规范 id 清单，用户暂缓）。② **过度打标**（个别论文一条 claim 堆 15–16 触点）→ 用"支持论文数"免疫。③ 审计需 glyph+HTML 归一化（已做）。

## 3. 当前状态（进行到哪一步）
P1+P2 全部跑完、矩阵干净可信。用户选**②直接进 P3**（跳过 claimId 根治，暂不重跑 41 篇）。**新会话从 P3 开始。**

## 4. 关键文件与路径（绝对路径）
| 绝对路径 | 作用 / 状态 |
|---|---|
| `/Users/augustsirius/Desktop/已清洗_260617/_虚拟细胞数据Perspective_Foundational-Context.md` | **地基 context**（thesis + 第3节7步逻辑链 + 第4节三轴 + 第5节 Claim 1–10）。P3 输入之一 |
| `/Users/augustsirius/Desktop/已清洗_260617/_build/evidence-matrix.md` | **★P3 主输入**：1391 触点按 claim 转置 + scoreboard + 逐字引文 + Other 主题 |
| `/Users/augustsirius/Desktop/已清洗_260617/_build/cards.json` | 41 张 v4 卡合并（excerpt-centric，下游唯一数据源） |
| `/Users/augustsirius/Desktop/已清洗_260617/_build/v4-cards/*_refined.json` | 41 张单卡（精练 v4 产物） |
| `/Users/augustsirius/Desktop/已清洗_260617/_build/citation-registry.md` | 引用号→书目 + `meta?`(33篇⚠待核验)。参考文献表种子+审计红线 |
| `/Users/augustsirius/Desktop/已清洗_260617/_build/build_matrix_v4.py` | P2 本体（确定性；含 canon()/逐字审计/过度打标探测）。改 refined 后重跑它即重建矩阵 |
| `/Users/augustsirius/Desktop/已清洗_260617/_build/compare_v3_v4.py` | v3↔v4 对比+逐字审计脚本 |
| `/Users/augustsirius/Downloads/精炼（Trial）/精练-kit/精练4.0-index.md` | **精练 v4 技能**（JSON 输出，今后新论文走这个） |
| `/Users/augustsirius/Downloads/精炼（Trial）/精练-kit/精练3.0-index.md` | 精练 v3（产出现有 41 篇 `_refined.md` 的权威版，勿删，对比用） |
| `/Users/augustsirius/Desktop/已清洗_260617/*_cleaned.md` / `*_refined.md` | 41 篇原文/提炼（keys 01–40，含 08a/08b） |
| `/Users/augustsirius/Desktop/已清洗_260617/_build/evidence-cards.P1-lossy.deprecated.json` / `build_matrix.P1.py` | **已弃用**的旧有损 P1 产物（改名留存，别用） |

## 5. 下一步 TODO（P3：用证据修正 thesis）
1. **派 1 个高推理 agent** 读 `_build/evidence-matrix.md` + 地基 context，产出 `/Users/augustsirius/Desktop/已清洗_260617/_build/thesis-revision.md`，内容：
   - 逐条裁决 context 第3节**7步逻辑链 + thesis**：confirmed / 需锐化 / 过度宣称 / 证据不足（用 scoreboard 的"支持论文数"做依据，别用触点数）。
   - 点出**必须正面处理的张力/反方**：论文 34（噪声 foil，Claim 4）、论文 03（model-first 对立论题，唯一 Other）。
   - 给出**精简后的主线骨架**（~2000 字够装的那几条）：哪条当头条、哪些降为支撑句、哪些砍。务必体现**空间 lavish / 时间 thin+contested 的不对称**（时间证据多是 workaround）。
   - 产出一版**修正后的 thesis 与逻辑链**。
2. **产出后停下来，让用户审**（P3 是观点step，必须人工过）。认可后再进 P4。
3. P4 成骨：每条论断绑 `[citationKey]` + 逐字原句 → `_build/outline.md`（可审计骨架）。P5 审计。编译。最后整包打成 `_local-skills` 可复用技能。

## 6. 关键命令 / 如何运行与验证
```bash
cd /Users/augustsirius/Desktop/已清洗_260617
python3 _build/build_matrix_v4.py        # 重建矩阵；打印 scoreboard + 三道质量闸
# 跑 P3 = 派一个 agent（Opus/高推理），输入 evidence-matrix.md + context，输出 _build/thesis-revision.md
```
- Workflow 工具坑：`args` 必须传真 JSON 数组（脚本已对字符串做防御 JSON.parse）；能用代码做的别派 agent。

## 7. 坑 / 注意事项 / 未决问题
- **canon() 是创可贴**：claimId 根治（context 加规范 id 清单 + 技能用精确键）被用户**暂缓**到 P3 之后/打包时再做。**别重跑 41 篇**（已花 ~274 万 token）。
- 判断 claim 支持强度**用"支持论文数"**，不用触点数（过度打标会灌水）。
- 精练 v4 决策已锁死：**无 strength / JSON 单产物 / 原句为中心**；已加抑制过度打标规则。
- **书目 33 篇待核验**（编译期硬任务，见 registry `meta?`）。
- 别覆盖 v3 的 `_refined.md`；v4 JSON 在 `_build/v4-cards/`。
- 桌面本应保持空（用户的文件组织规范）；但本项目数据就在 Desktop/已清洗_260617，**别擅自搬动**。

## 8. 给接力者的话
先读 `_build/evidence-matrix.md`（重点看 scoreboard、Claim 3 / Axis B 两行、和末尾 Other=论文03）+ context 文件，**再**派一个高推理 agent 跑 P3 产出 `_build/thesis-revision.md`，然后**停下让用户审**再往下。用户要被带着走、每个决策点讲清楚、中文交流。最易翻车点：别把"触点数多"当"证据强"，别忘了时间轴才是 thesis 真正的锋刃。
