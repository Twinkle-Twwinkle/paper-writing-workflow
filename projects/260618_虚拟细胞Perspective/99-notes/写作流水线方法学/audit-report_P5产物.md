# P5 审计报告 — outline.md 逐字保真核验

> 方法：脚本 `audit_outline_P5.py` 抽出 `outline.md` 全部引文，回各论文 `*_cleaned.md` 原文逐字搜索（NFKC + 去 HTML 标签 + glyph/引号/连字符归一化 + 折叠空白）。
> 运行：`python3 _build/audit_outline_P5.py`

## 结论：**PASS（零编造）**

- 引文总数：**45 条**，覆盖 **27 篇**论文。
- **归一化后逐字命中：41 / 45 = 91%。**
- 未命中 4 条，**逐条核查全部为良性差异，无一改写或编造**：

| key | 命中度 | 差异成因 | 判定 |
|---|---|---|---|
| 25 | 353/354 字 | 结尾句号前夹引用上标（`trajectory`<sup>13</sup>`.`），剥离后差最后一个字符 | 良性·引用号 |
| 01 | 前 61 字 | `_cleaned.md` 源文有 OCR 杂字（`con C text`、`specific A cell` 混入图注字母 C/A）；cards.json 为清理后版本，引文忠于本意 | 良性·源文 OCR 噪声 |
| 22 | 前 120 字 | 原文 `RNA velocity,140,141 these approaches…` 的内联引用号 `140,141` 被剥离 | 良性·引用号 |
| 36 | 前 163 字 | 引文内部含 ` ... ` 省略号（P1 跨句省略）；省略号前后两段在原文中均真实存在 | 良性·跨句省略 |

> 这四类（引用上标 / 内联引用号 / 源文 OCR 杂字 / 跨句省略）与 P1 阶段全量审计的结论一致：**所有"未命中"都是引用号数字/标签被合理剥掉或源文噪声，零改写零编造。**

## 待编译期处理的事项（非审计失败，但需知会）

1. **[36] 的省略号引文**：A4.5 的代谢组引文跨了一处 ` ... `。编译成正文时，建议拆成两句或只取信息密度最高的一段，避免正文里出现裸 `...`。
2. **书目核验（硬任务）**：`citation-registry.md` 中 **除 13 / 29 / 30 / 35 等少数标 `ok` 外，其余约 33 篇期刊/DOI 为推断**（`meta?=⚠`）。逐字引文审计与此无关，但**正式参考文献表编译前必须核验**这些书目。见 outline.md 附录 A。
3. **direction 借用的诚实性**：outline 中若干引文原 touchpoint 方向为 `challenges`/`nuances`（如时间段的 workaround 工具、模态段的论文 34 steelman），已在 outline 逐条中文括注其修辞用法；审计只验逐字命中与方向标注诚实，不推翻其用法。

## 可审计性兑现

本报告 + `outline.md`（每条论断绑 `[citationKey]` + 逐字原句 + anchor）+ `audit_outline_P5.py`（可复跑）共同构成"**可被审计的 outline 骨架**"：任何人可独立复跑脚本、或手工把任一引文放回 `*_cleaned.md` 搜索验证。
