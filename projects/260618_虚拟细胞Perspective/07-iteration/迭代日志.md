# 迭代日志（Iteration Log）

CURRENT: v1

> 顶部 `CURRENT` 永远指向「当前被接受的版本」。每轮迭代在下方追加一条，从新到旧。
> 材料回写 `00–06`，本文件只记账。版本目录：`06-outline-and-draft/vN/`。

---

## v1 · 2026-06-18 · 首版（from 无）
- **输入 context**：`00-context/context.md`（v1 地基，AIVC 算据标准主线）。
- **本轮重跑**：00 → 06 全流程跑通一轮（自建流水线，方法学见 `99-notes/写作流水线方法学/`）。
- **文献**：51 篇（V1 40 篇 + V2 补充 11 篇）已下载→清洗→精练为 `05-distilled/cards.json`（41 张卡 / 1003 excerpts / 1391 touchpoints）。
- **产物**：`06-outline-and-draft/v1/outline.md`（45 论断可审计骨架）+ `v1/draft.md`（1714 字英文成稿）。
- **审计结论**：P5 逐字回搜 PASS、零编造。
- **状态**：已接受为 v1 基线；待按 v2 新意见回炉。

---

## 📌 下一轮计划（v2，尚未开跑）
- **触发 context**：`00-context/郭天南新增意见汇总_260622.md`（两条意见）。
- **本轮预计重跑**：00（新意见已存档）→ 06；**01–05 跳过**（支撑 VC≠DT 的 11 篇 V2 文献已蒸馏，无新增文献）。
- **改动方式（已与作者敲定）**：在 v1 基础上**最小改动**，于 `¶1 开场后加一段「范围界定」**，并入：
  - **AIVC ≠ scRNA single cell**（老师定性「极其重要，尽快写出来」）；
  - **Virtual Cell ≠ Digital Twin**（用 `*/V2补充论文/` 的 11 篇做支撑）。
- **产物（待出）**：`06-outline-and-draft/v2/{outline,draft}.md`，旧版 v1 只读保留。
- **登账（待补）**：本轮跑完在此条上方追加 `## v2 · ... · from v1`，并把顶部 `CURRENT` 改为 `v2`。

---

## 🧹 已知债务（择机清，不挡迭代）
- `05-distilled/cards.json` 的 `claimId` 早期多 agent 各自发明、未统一（详见 `00-context/evidence-questions.md` 文末）。下次碰 05 时把 claimId 归一化到 `evidence-questions.md` 规范 key。
- 书目核验：`_inspection/citation-registry.md` 中约 33 篇 `meta?=⚠`（推断的 DOI/期刊），最终编译参考文献前须逐条核实。
- 各步 README 尚未全部统一成「输入/人手动启动/输出/人工核验/通过后进入」五栏模板（导师建议二），可下一轮补。
